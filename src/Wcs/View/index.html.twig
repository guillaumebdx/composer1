{% extends 'base.html.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
        {% for produit in produits %}
            <div class="card col-4" style="width: 18rem;">
                <div class="card-body">
                    <h2>{{ produit}}</h2>
                </div>
            </div>
        {% endfor %}
        </div>
    </div>

{% endblock %}